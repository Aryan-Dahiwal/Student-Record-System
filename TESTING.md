## Edge Case Testing

1. Search non-existent roll → prints "not found" message 
2. Delete from empty list → prints "no records" message
3. Sort with 1 student → no crash, prints sorted message 
4. Add → sort → delete → display works correctly 